<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.admin.member.AdminMemberMapper">

    <!-- 사용자 목록 조회 -->
    <select id="selectUserList" resultType="kr.co.sist.user.member.UserDomain">
        SELECT USER_ID, NAME, EMAIL, PHONE, REGDATE, REGIP
        FROM USERS
        ORDER BY REGDATE DESC
    </select>
    
    <!-- 강사 목록 조회 -->
    <select id="selectInstructorList" resultType="kr.co.sist.instructor.member.InstructorDomain">
        SELECT INST_ID, NAME, EMAIL, PHONE, REGDATE, REGIP, APPROVAL, ACTIVATION
        FROM INSTRUCTOR
        ORDER BY REGDATE DESC
    </select>
    
    <!-- 강사 승인 -->
    <update id="updateInstructorStatus" parameterType="String">
        UPDATE INSTRUCTOR
        SET APPROVAL = 1
        WHERE INST_ID = #{instId}
    </update>

</mapper>
